services:
  web:
    build: .
    image: voxcpm-web
    container_name: voxcpm-web
    env_file: .env
    ports:
      - "${PORT:-8484}:${PORT:-8484}"
    environment:
      HOST: 0.0.0.0
      PORT: ${PORT:-8484}
    restart: unless-stopped
